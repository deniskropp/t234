---
description: Provide a summary of the current state and wait for further user instructions.
---

1. List the contents of the `tasks/` directory and report on the status of each task, logging to `output/system_tasks_status.md`.
2. Check for any active or pending background commands and report their status, logging to `output/system_background_commands.md`.
3. Summarize the latest architectural changes or research findings, logging to `output/system_contexts.md`.
4. Suggest next steps or tasks to perform, logging to `output/system_suggestions.md`.
5. Signal readiness and explicitly state that you are waiting for the user's next request via `notify_user`.
