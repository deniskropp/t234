# Command: ask

## Overview
The `ask` command provides a natural language interface to the codebase, allowing users to query information, generate code, or perform refactoring tasks.

## Implementation Details
- **CLI Layer**: `src/cli/ask.py` handles user input and output formatting using `rich`.
- **Service Layer**: `src/shared/
